// Drop this static block into the copy's main Unity activity
// (usually smali: com/unity3d/player/UnityPlayerActivity).

static {
    try {
        System.loadLibrary("TvMenu");
    } catch (Throwable ignored) {
    }
}
